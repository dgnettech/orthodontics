# Supabase is used only to persist contact and appointment form submissions.
# The website still builds without these values, but forms will show a setup message until configured.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-server-only-service-role-key
